name: slvh_app
description: "SLVH Smart Shop"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  firebase_storage: ^13.3.0
  firebase_messaging: ^16.2.0
  flutter_local_notifications: ^19.5.0
  go_router: ^15.1.2
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  cached_network_image: ^3.3.1
  photo_view: ^0.15.0
  image_picker: ^1.0.8
  flutter_image_compress: ^2.3.0
  file_picker: ^8.1.7
  qr_flutter: ^4.1.0
  fl_chart: 0.66.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
