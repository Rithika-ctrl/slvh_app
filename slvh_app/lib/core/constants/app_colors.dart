import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ──────────────────────────────────────────────
  static const Color bgCream      = Color(0xFFFDF6EB);
  static const Color bgCreamLight = Color(0xFFF0E6D0);

  // ── Orange accent ─────────────────────────────────────────────
  static const Color orange       = Color(0xFFE07800);
  static const Color orangeLight  = Color(0xFFFF9F43);
  static const Color orangePale   = Color(0xFFFFF8EE);

  // ── Card ──────────────────────────────────────────────────────
  static const Color cardBg       = Color(0xE0FFFFFF);
  static const Color cardBorder   = Color(0x38DCA03C);

  // ── Text ──────────────────────────────────────────────────────
  static const Color textDark     = Color(0xFF2D1B00);
  static const Color textMid      = Color(0xFF6B4226);
  static const Color textMuted    = Color(0xFFA06010);
  static const Color textHint     = Color(0xFFC0A060);

  // ── Status ────────────────────────────────────────────────────
  static const Color success      = Color(0xFF00B894);
  static const Color error        = Color(0xFFE17055);
  // Deprecated alias used across older UI code
  static const Color danger       = error;
  static const Color warning      = Color(0xFFFDCB6E);

  // ── Category colours ──────────────────────────────────────────
  static const Color catBlue      = Color(0xFF0984E3);
  static const Color catPink      = Color(0xFFE84393);
  static const Color catGreen     = Color(0xFF00B894);
  static const Color catYellow    = Color(0xFFE17055);
  static const Color catPurple    = Color(0xFF6C5CE7);
  static const Color catCyan      = Color(0xFF00CEC9);

  // ── Aliases for admin screens ─────────────────────────────────
  static const Color cyan          = Color(0xFF00B8A9);
  static const Color textPrimary   = Color(0xFF2D1B00);
  static const Color textSecondary = Color(0xFF6B4226);
}